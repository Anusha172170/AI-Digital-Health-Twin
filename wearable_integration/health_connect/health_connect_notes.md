# Android connector implementation gate

Pending user's Android version, wearable model and companion app. Confirm the app actually writes the required records to Health Connect. Implement native permission handling, read records with source metadata, incremental sync and permission revocation handling; normalize into the common observation contract. Test on a physical Android device. No SDK integration exists yet.
